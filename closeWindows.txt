tell application "Terminal"
    activate
    windows where name contains "localManager"
    if result is not {} then set index of item 1 of result to 1
    set mainID to id of front window
    close (every window whose id ≠ mainID)
end tell